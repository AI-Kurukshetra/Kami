# Mobile App (Low Priority)

This folder is reserved for the React Native app.

Current priority is web MVP completion (`apps/web`) with Supabase and Vercel.
Mobile implementation will start after web MVP stabilization.

Planned scope for mobile phase:
- Auth/session parity with web
- Read-focused document access first
- Collaboration actions in later mobile increments
